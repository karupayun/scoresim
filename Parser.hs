-- Modulo Parser
--   Parsea un scoreboard de una competencia pasada, y transforma la información 
--      en un formato deseable, para luego ser trabajada. 
--   Simplemente importa un parser específico para leer un scoreboard con un formato dado.

module Parser (teamsParseados) where
import ParserKattis as P (teamsParseados)
